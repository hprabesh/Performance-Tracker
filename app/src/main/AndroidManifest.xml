<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.performance_tracker">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Performancetracker">
        <activity android:name=".view_friend_streak"></activity>
        <activity android:name=".LoadAllTasks" />
        <activity android:name=".AddClass" />
        <activity android:name=".MarkCompleteTask" />
        <activity android:name=".streak_history" />
        <activity android:name=".AddTask" />
        <activity android:name=".AddProject" />
        <activity android:name=".AddFriends" />
        <activity android:name=".ForgetPassword" />
        <activity android:name=".Profile" />
        <activity android:name=".Registration" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>